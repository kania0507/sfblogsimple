{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
		<h1>{{ post.title }}</h1>
		<p>{{ post.content }}</p>
		<span>Dodano: {{ post.created|date('H:i d/m/Y')}}</span>

        </div>
    </div>
{% endblock %}

