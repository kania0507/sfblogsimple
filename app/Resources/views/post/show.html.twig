{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
		<h1>{{ post.title }}</h1>
		<p>{{ post.content }}</p>
		

        </div>
    </div>
{% endblock %}

