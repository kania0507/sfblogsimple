{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        
			glowna<br />
			
			{% for post in posts %}
				<div class="col-md-4 col-sm-12">
					<h1><a href="{{ path('post_show', {id: post.id}) }}">{{ post.title }}</a></h1>
					<p>{{ post.content }}</p>
				</div>
			{% endfor%}
			{{ knp_pagination_render(posts) }}
    </div>
{% endblock %}
